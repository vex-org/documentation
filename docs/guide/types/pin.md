# Pin documentation moved

`Pin` is a compiler-enforced marker contract, not a generic `Pin<T>` type.

See [Pin — Move Prevention for Self-References](/guide/memory/pin) for the
current documentation.
