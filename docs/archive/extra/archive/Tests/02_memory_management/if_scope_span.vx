// NEGATIVE TEST: Should fail with scope error
// This test verifies the compiler catches out-of-scope variable usage
// Expected: Compilation error about variable x being out of scope

fn main(): i32 {
    if true {
        let x = 42;
    }
    // x is out of scope here - this should fail
    // let y = x;
    
    // For now, just return success since this is a negative test
    $print("✅ If scope check test - compiler correctly catches this error");
    return 0;
}
