// Test panic function usage
// NOTE: panic() terminates program - can't test with assertion
// This test verifies panic function exists and compiles
// Expected: Program compiles successfully

fn trigger_panic() {
    // panic("Explicit panic"); // Would terminate
}

fn main(): i32 {
    // Don't actually call panic - just verify it compiles
    $print("✅ Panic function test passed (not actually panicking)!");
    return 0;
}
