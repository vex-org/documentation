import{_ as o,o as t,c as d,ag as c}from"./chunks/framework.BGkQCay2.js";const h=JSON.parse('{"title":"Collections API reference","description":"","frontmatter":{},"headers":[],"relativePath":"std/collections_reference.md","filePath":"std/collections_reference.md"}'),r={name:"std/collections_reference.md"};function a(i,e,n,l,s,p){return t(),d("div",null,[...e[0]||(e[0]=[c('<h1 id="collections-api-reference" tabindex="-1">Collections API reference <a class="header-anchor" href="#collections-api-reference" aria-label="Permalink to &quot;Collections API reference&quot;">​</a></h1><p>Date: 2026-08-25</p><p>The canonical low-level containers <code>Vec&lt;T&gt;</code>, <code>Map&lt;K,V&gt;</code>, <code>Set&lt;T&gt;</code>, <code>OrderedMap&lt;K,V&gt;</code>, and <code>Deque&lt;T&gt;</code> are VexArch prelude types. The <code>collections</code> package exports <code>Stack&lt;T&gt;</code>, <code>PriorityQueue&lt;T&gt;</code>, and <code>LinkedList&lt;T&gt;</code>.</p><h2 id="map-k-v-and-set-t" tabindex="-1"><code>Map&lt;K,V&gt;</code> and <code>Set&lt;T&gt;</code> <a class="header-anchor" href="#map-k-v-and-set-t" aria-label="Permalink to &quot;`Map&lt;K,V&gt;` and `Set&lt;T&gt;`&quot;">​</a></h2><p>No import is required.</p><ul><li><code>Map.get</code> / <code>getMut</code>: borrow a value.</li><li><code>Map.getKeyValue</code>: borrow the canonical stored key and value with one probe.</li><li><code>Map.insert</code>: transfer key and value; replace an existing value in place.</li><li><code>Map.remove</code>: transfer the value and drop the stored key.</li><li><code>Map.removeEntry</code>: transfer both stored owners.</li><li><code>Map.getOrInsertWith</code>: invoke a zero-argument factory only on a miss.</li><li><code>Map.withCapacity</code> / <code>withHasherCapacity</code>: guarantee element capacity at the 7/8 load limit; <code>capacity</code>, <code>reserve</code>, <code>retain</code>, and <code>shrinkToFit</code> use element counts. Legacy <code>withCap</code> / <code>cap</code> explicitly use raw bucket counts.</li><li><code>Set.get</code>: borrow the stored representative; <code>Set.take</code>: transfer it out.</li><li><code>Set.capacity</code> / <code>reserve</code> / <code>retain</code> / <code>shrinkToFit</code>: delegate to the same canonical Map storage.</li></ul><p>Map probing uses portable Vex fixed-array/Mask SIMD; there is no C hash-table shim or container-name-specific compiler lowering.</p><h2 id="orderedmap-k-v" tabindex="-1"><code>OrderedMap&lt;K,V&gt;</code> <a class="header-anchor" href="#orderedmap-k-v" aria-label="Permalink to &quot;`OrderedMap&lt;K,V&gt;`&quot;">​</a></h2><p>No import is required.</p><ul><li><code>new</code>, <code>withCapacity</code>, <code>withHasher</code>, <code>withHasherCapacity</code>: owning insertion-ordered construction.</li><li><code>insert</code>: return whether the key is new; an update preserves the canonical stored key and order position.</li><li><code>get</code>, <code>getMut</code>, <code>getKeyValue</code>, <code>contains</code>: borrowed average O(1) lookup.</li><li><code>removeValue</code>, <code>removeEntry</code>, <code>remove</code>: value transfer, pair transfer, or boolean dropping removal.</li><li><code>iter</code>, <code>keys</code>, <code>values</code>: allocation-free borrowed current-order traversal.</li><li><code>first</code>, <code>last</code>, <code>popFront</code>, <code>popBack</code>: O(1) order-end access/transfer.</li><li><code>moveToFront</code>, <code>moveToBack</code>: average O(1) lookup and constant-time relink.</li><li><code>capacity</code>, <code>reserve</code>, <code>retain</code>, <code>clear</code>, <code>shrinkToFit</code>: stable-slot capacity and mutation without key cloning; compaction reuses cached hashes.</li><li><code>getAtIndex</code>, <code>getAtIndexRef</code>, <code>getAtIndexMut</code>: compatibility current-order access in O(min(i,n-i)).</li></ul><p>The type is insertion-ordered rather than key-sorted. Sorted range maps/sets remain a separate future B-tree family.</p><h2 id="deque-t" tabindex="-1"><code>Deque&lt;T&gt;</code> <a class="header-anchor" href="#deque-t" aria-label="Permalink to &quot;`Deque&lt;T&gt;`&quot;">​</a></h2><p>No import is required.</p><table tabindex="0"><thead><tr><th>API</th><th>Semantics</th></tr></thead><tbody><tr><td><code>Deque.new&lt;T&gt;()</code></td><td>Empty owning deque with a small initial capacity.</td></tr><tr><td><code>Deque.withCapacity&lt;T&gt;(n)</code></td><td>Empty deque; <code>n == 0</code> performs no allocation.</td></tr><tr><td><code>pushFront(value)</code> / <code>pushBack(value)</code></td><td>Transfer ownership into either end; amortized O(1).</td></tr><tr><td><code>popFront()</code> / <code>popBack()</code></td><td>Transfer ownership out through <code>Option&lt;T&gt;</code>; amortized O(1).</td></tr><tr><td><code>front()</code> / <code>back()</code></td><td>Borrow an end through <code>Option&lt;&amp;T&gt;</code>.</td></tr><tr><td><code>get(index)</code></td><td>Borrow a logical front-relative index.</td></tr><tr><td><code>frontMut()</code> / <code>backMut()</code> / <code>getMut(index)</code></td><td>Exclusively borrow a live value.</td></tr><tr><td><code>len()</code> / <code>isEmpty()</code> / <code>capacity()</code></td><td>Constant-time state queries.</td></tr><tr><td><code>reserve(minimum)</code></td><td>Ensure total capacity; preserves order and allocator region.</td></tr><tr><td><code>shrinkToFit()</code></td><td>Reclaim unused heap capacity; arena allocations remain bulk-owned.</td></tr><tr><td><code>clear()</code></td><td>Drop every live owner exactly once and retain storage.</td></tr><tr><td><code>iter()</code></td><td>Borrow front-to-back without allocation.</td></tr><tr><td><code>asSlices()</code></td><td>Borrow logical contents as one or two contiguous <code>Span&lt;T&gt;</code> values.</td></tr></tbody></table><h2 id="stack-t" tabindex="-1"><code>Stack&lt;T&gt;</code> <a class="header-anchor" href="#stack-t" aria-label="Permalink to &quot;`Stack&lt;T&gt;`&quot;">​</a></h2><p>Import <code>Stack</code> from <code>collections</code>. It exposes <code>new</code>, <code>withCapacity</code>, <code>push</code>, <code>pop</code>, borrowed <code>peek</code>, exclusive <code>peekMut</code>, <code>truncate</code>, <code>len</code>, <code>isEmpty</code>, <code>capacity</code>, <code>reserve</code>, <code>shrinkToFit</code>, <code>clear</code>, and a borrowed bottom-to-top <code>iter</code> over canonical <code>Vec&lt;T&gt;</code> storage.</p><h2 id="priorityqueue-t-p-maxpriority-t" tabindex="-1"><code>PriorityQueue&lt;T, P = MaxPriority&lt;T&gt;&gt;</code> <a class="header-anchor" href="#priorityqueue-t-p-maxpriority-t" aria-label="Permalink to &quot;`PriorityQueue&lt;T, P = MaxPriority&lt;T&gt;&gt;`&quot;">​</a></h2><p>Import <code>PriorityQueue</code>; custom policies additionally import <code>PriorityOrder</code>.</p><ul><li><code>new</code> / <code>withCapacity</code>: max heap.</li><li><code>min</code> / <code>minWithCapacity</code>: min heap.</li><li><code>fromVec</code> / <code>minFromVec</code>: consuming O(n) Floyd heap construction.</li><li><code>withOrder</code> / <code>withOrderCapacity</code> / <code>fromVecWithOrder</code>: monomorphized custom ordering with no runtime comparator pointer.</li><li><code>push</code> / <code>pop</code>: O(log n); <code>peek</code>: O(1).</li><li><code>removeAt</code>: remove a heap-layout owner and repair in O(log n).</li><li><code>tryReplaceAt</code>: replace and repair, or return the supplied owner in <code>Err</code>.</li><li><code>replaceTop</code>: replace the selected extremum and sift once.</li><li><code>pushPop</code>: fused fixed-length push-then-pop semantics.</li><li><code>append</code>: transfer another heap and rebuild once in O(n+m).</li><li><code>retain</code>: evaluate once per owner and rebuild in O(n).</li><li><code>drainPriority</code>: transfer owners in priority order without a second buffer.</li><li><code>intoPriorityVec</code>: consume the queue and reuse its allocation for priority-ordered materialization.</li><li><code>iter</code>: borrow heap layout; iteration is intentionally not priority-sorted.</li></ul><h2 id="linkedlist-t" tabindex="-1"><code>LinkedList&lt;T&gt;</code> <a class="header-anchor" href="#linkedlist-t" aria-label="Permalink to &quot;`LinkedList&lt;T&gt;`&quot;">​</a></h2><p>Import <code>LinkedList</code> from <code>collections</code>. <code>pushFront</code>, <code>pushBack</code>, <code>popFront</code>, and <code>popBack</code> are O(1); <code>peekFront</code> and <code>peekBack</code> borrow. <code>clear</code> retains the stable-index node pool, <code>peekFrontMut</code> / <code>peekBackMut</code> borrow exclusively, and <code>iter</code> walks front-to-back.</p><p>All insertion APIs transfer ownership, all removal APIs transfer it back, and borrowed queries allocate nothing. None of these APIs requires application developers to manage reference counts or thread lifetimes.</p>',22)])])}const m=o(r,[["render",a]]);export{h as __pageData,m as default};
