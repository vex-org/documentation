import{_ as a,o as t,c as i,ag as s}from"./chunks/framework.BGkQCay2.js";const u=JSON.parse('{"title":"SIMD / SIMT Unified Execution Contract","description":"","frontmatter":{},"headers":[],"relativePath":"guide/simd/simd-simt-contract.md","filePath":"guide/simd/simd-simt-contract.md"}'),n={name:"guide/simd/simd-simt-contract.md"};function o(d,e,r,c,l,p){return t(),i("div",null,[...e[0]||(e[0]=[s(`<h1 id="simd-simt-unified-execution-contract" tabindex="-1">SIMD / SIMT Unified Execution Contract <a class="header-anchor" href="#simd-simt-unified-execution-contract" aria-label="Permalink to &quot;SIMD / SIMT Unified Execution Contract&quot;">​</a></h1><p>Vex uses one user-visible compute model for both CPU vectorization and GPU execution.</p><p>The goal is simple:</p><ul><li>write normal Vex code (<code>array</code> / <code>Tensor&lt;T,N&gt;</code> / <code>Tensor&lt;T&gt;</code> / <code>Span&lt;T&gt;</code>)</li><li>keep semantics in one place</li><li>let runtime pick CPU SIMD, SIMD-like CPU kernels, or GPU SIMT backend</li></ul><p>There is <strong>no separate “SIMT API”</strong> in user code; kernel concepts come from the same algebraic operations and the same <code>Tensor</code>/<code>Mask</code> abstractions.</p><h2 id="_1-core-cross-backend-contract" tabindex="-1">1) Core cross-backend contract <a class="header-anchor" href="#_1-core-cross-backend-contract" aria-label="Permalink to &quot;1) Core cross-backend contract&quot;">​</a></h2><p>Every operation category below has the same source-level meaning independent of backend:</p><ul><li><code>Map</code> (unary/binary element-wise ops): <code>f(a)</code> / <code>f(a,b)</code></li><li><code>Reduce</code>: <code>sum</code>, <code>prod</code>, <code>min</code>, <code>max</code>, <code>all</code>, <code>any</code>, etc.</li><li><code>Compare</code>: <code>==</code>, <code>&lt;</code>, <code>&gt;</code>, <code>&lt;=</code>, <code>&gt;=</code></li><li><code>Mask</code> creation + mask algebra: <code>&amp;</code>, <code>|</code>, <code>^</code>, <code>~</code></li><li><code>Select</code>: <code>cond ? a : b</code> / <code>mask.select(a,b)</code></li><li><code>Index</code>: <code>gather</code>, <code>scatter</code>, <code>scatter_add</code>, <code>scatter_max</code>...</li><li><code>Prefix/scan</code> style kernels used in higher-level algorithms</li></ul><p>The compiler emits the same semantic node for both backends where possible; execution engine choice is done only by dispatch policy and capability.</p><h2 id="_2-ownership-and-shape-contract" tabindex="-1">2) Ownership and shape contract <a class="header-anchor" href="#_2-ownership-and-shape-contract" aria-label="Permalink to &quot;2) Ownership and shape contract&quot;">​</a></h2><ul><li><code>Mask&lt;N&gt;</code> and <code>Tensor&lt;T, N&gt;</code> represent <strong>fixed-shape</strong> values and are suitable for register-level codegen when width/shape allow it.</li><li><code>DynMask</code> / <code>Tensor&lt;T&gt;</code> represent <strong>runtime-shape</strong> values. <code>DynMask</code> stores packed bits and an owner field; it is not a <code>{ptr,len}</code> byte slice.</li><li>Scalar forms always stay scalar. Vectorized backends must not change user-visible scalar behavior.</li><li><code>Span&lt;T&gt;</code> is a borrowed view; moving into owned tensor values follows the ownership rules documented in memory sections.</li></ul><h2 id="_3-cpu-simd-vs-gpu-simt-behavior" tabindex="-1">3) CPU SIMD vs GPU SIMT behavior <a class="header-anchor" href="#_3-cpu-simd-vs-gpu-simt-behavior" aria-label="Permalink to &quot;3) CPU SIMD vs GPU SIMT behavior&quot;">​</a></h2><ul><li><strong>SIMD backend</strong> = register/vector execution (<code>+simd</code> style codegen, loop-vectorized kernels, and fixed-width tensor lowering).</li><li><strong>SIMT backend</strong> = one lane/thread lane mapping (GPU warp/wavefront model).</li></ul><p>They differ in scheduling:</p><ul><li>register width / lane count</li><li>memory model (<code>cache</code> vs <code>threadgroup</code>/<code>global</code>)</li><li>launch granularity (<code>for</code>-body vs kernel/thread semantics)</li></ul><p>They <strong>must not</strong> differ in:</p><ul><li>operator identities</li><li>mask semantics</li><li>comparison truth tables</li><li>overflow and clamp policies</li><li>ownership transitions (when a result is borrowed vs owned)</li></ul><h2 id="_4-what-same-interface-looks-like-in-practice" tabindex="-1">4) What &quot;same interface&quot; looks like in practice <a class="header-anchor" href="#_4-what-same-interface-looks-like-in-practice" aria-label="Permalink to &quot;4) What &quot;same interface&quot; looks like in practice&quot;">​</a></h2><p>The same source stays identical:</p><div class="language-vex vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vex</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// same user code for both fallback SIMD and GPU path</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">fn</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> relu</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Tensor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">f32</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Tensor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">f32</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> x</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">max</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">fn</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> keep_positive</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Tensor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">f32</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Tensor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">f32</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> m </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.0</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> m</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">select</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>If a backend cannot support the exact fast path, it must preserve correctness and fall back to a valid implementation of the same contract instead of changing semantics.</p><h2 id="_5-backend-capability-matrix-contracted" tabindex="-1">5) Backend capability matrix (contracted) <a class="header-anchor" href="#_5-backend-capability-matrix-contracted" aria-label="Permalink to &quot;5) Backend capability matrix (contracted)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>API family</th><th>CPU SIMD (fixed + dynamic)</th><th>GPU SIMT (Metal/CUDA/ROCm/SPIR-V/WGSL)</th><th>Contract requirement</th></tr></thead><tbody><tr><td>Map (<code>+</code>, <code>*</code>, custom <code>Math.*</code>)</td><td>Yes</td><td>Yes</td><td>elementwise identity must match</td></tr><tr><td>Compare + <code>Mask</code></td><td>Yes</td><td>Yes</td><td>same boolean result layout rules per type</td></tr><tr><td>Reduce (<code>+</code>, <code>*</code>, <code>min</code>, <code>max</code>, <code>all</code>, <code>any</code>)</td><td>Yes</td><td>Yes</td><td>scalar return type and NaN/compare policy aligned</td></tr><tr><td>Select (<code>cond ? a : b</code>)</td><td>Yes</td><td>Yes</td><td>predicated result for each logical lane/thread</td></tr><tr><td>Gather/Scatter</td><td>Yes (where supported)</td><td>Yes (where supported)</td><td>deterministic indexing semantics</td></tr><tr><td>Mask bit operations</td><td>Yes</td><td>Yes</td><td>same logical truth tables</td></tr><tr><td>Prefix scan / cumulative ops</td><td>Partial</td><td>Partial</td><td>keep same algebraic associativity expectations</td></tr><tr><td><code>Mask.compress</code></td><td>Yes: LLVM vector intrinsic or legal register network</td><td>native subgroup/warp/wave fast path plus bounded portable fixed-mask legalization</td><td>stable selected prefix, exact count, deterministic zero tail</td></tr></tbody></table><h2 id="_6-lane-width-and-compaction" tabindex="-1">6) Lane width and compaction <a class="header-anchor" href="#_6-lane-width-and-compaction" aria-label="Permalink to &quot;6) Lane width and compaction&quot;">​</a></h2><p><code>#Target.simdLanes&lt;T&gt;()</code> reports the logical lane count selected for <code>T</code> at compile time. The meaning is shared, while the physical executor differs:</p><ul><li>on CPU it is the number of <code>T</code> elements in the selected native vector width,</li><li>on CUDA and Metal it is the native 32-lane warp/simdgroup width,</li><li>on ROCm it follows the selected device&#39;s wave32/wave64 profile,</li><li>on portable SPIR-V/WGSL it is unavailable until a concrete device profile guarantees subgroup capabilities and width.</li></ul><p>The last case is deliberately fail-closed. A <code>#</code> intrinsic cannot truthfully return a subgroup size that is known only after runtime adapter selection.</p><p>For dynamically routed <code>graph fn</code> code, lane-dependent graph specialization must therefore happen after the backend/device profile is selected. A lane constant produced for the CPU variant must not be reused by a GPU variant. HIR lowers the exact intrinsic identity to a symbolic SIR <code>ExecutionLanes</code> node. The selected backend then clones the graph and replaces that node with a constant: element-width-aware on CPU, 32 on CUDA/Metal, wave32/wave64 on ROCm, or the exact adapter subgroup width for SPIR-V/WGSL. The source graph is never mutated, so independently compiled CPU and GPU variants cannot leak constants into each other.</p><p>Inside <code>graph fn</code>, use the value as an ordinary expression. It is not legal in <code>#if</code> or <code>#const</code>, because backend/device routing happens after graph creation. The backend-specialized value is still emitted as a constant and has no runtime query cost.</p><p>For a fixed <code>Mask&lt;N&gt;</code> and <code>[T; N]</code>, <code>mask.compress(values)</code> has one definition on every backend:</p><div class="language-vex vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vex</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (packed, count) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> mask</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">compress</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(values)</span></span></code></pre></div><ul><li>selected values occupy <code>packed[0..count]</code> in their original order,</li><li><code>packed[count..N]</code> is deterministically zero,</li><li><code>count</code> is <code>usize</code>,</li><li>inactive physical GPU lanes do not participate,</li><li>and a mask wider than one native subgroup uses a bounded whole-mask legalization; a backend never presents independent subgroup compactions as one result.</li></ul><p>Current SIR lowering covers CPU scalar/SIMD, Metal, CUDA, ROCm, and SPIR-V/WGSL. Profiled SPIR-V/WGSL compaction uses subgroup arithmetic for an exact stable rank plus a subgroup barrier; it does not pay for a ballot unless an actual bitmask is requested. The WebGPU runtime requests <code>SUBGROUP</code> and <code>SUBGROUP_BARRIER</code> and reads the selected adapter&#39;s subgroup range. An exact range (<code>min == max</code>) can specialize <code>simdLanes</code>; a variable range cannot. It can still run the native <code>compress</code> path when <code>N &lt;= min</code>. Without that proof—or when the fixed mask is wider—the backend emits a bounded portable whole-mask kernel. This does not manufacture a <code>simdLanes</code> constant and therefore keeps the compile-time query honest while preserving operation semantics.</p><p>The standalone-kernel boundary validates this structurally before any backend sees the graph: <code>Mask&lt;N&gt;</code> is fixed to <code>1..=64</code>, operands and results must have matching lane counts and dtypes, and unrelated SIR nodes or extra buffers are rejected instead of being silently discarded. <code>toBitmask</code> uses one <code>i64/u64</code> transport scalar. Packed <code>Int4</code>/<code>Fp4</code> compaction uses two nibbles per logical host byte and eight nibbles per canonical GPU <code>u32</code> word. Conversion is owned by the descriptor boundary, so no backend can reinterpret packed values as byte lanes.</p><p><code>Mask</code> also has one transport contract. Its source and host representation remains abstract (fixed masks may be packed), while GPU storage buffers use one <code>u32</code> per lane because WGSL/SPIR-V do not permit boolean storage-buffer lanes. The shared SIR runtime expands packed or byte-per-lane inputs at the GPU boundary and restores mask outputs to logical one-byte values. This is an internal ABI detail: CUDA, ROCm and Metal source code does not acquire a different <code>Mask</code> API.</p><p>The versioned execution descriptor carries the exact kernel representation, entry point, ordered inputs and outputs, per-buffer logical encoding, and grid/workgroup geometry. Metal, WebGPU and CUDA consume this same contract; backend-specific positional FFI conventions are not part of <code>Mask</code> or <code>Tensor</code> semantics.</p><p>Kernel representation is selected by the concrete executor, not by the host OS: a Metal executor requests MSL, while a WebGPU executor requests SPIR-V even when WebGPU is the fallback on macOS. This keeps device routing and subgroup specialization attached to the same capability source.</p><p>Metal, CUDA and ROCm lower the canonical <code>(packed, count)</code> result as one real standalone kernel with inputs followed by outputs in the runtime binding order. Each selected lane computes a stable rank, the lane group clears the tail, synchronizes once, and writes the selected prefix. Metal output is compiled by Apple&#39;s shader compiler in the regression suite; CUDA PTX and ROCm HIP receive structural codegen tests and require their native toolchains/hardware in the platform CI matrix.</p><p>Codegen parity and runtime-routing parity are deliberately separate. Metal, WebGPU and feature-gated CUDA PTX implement the common descriptor-based SIR graph executor. CUDA honors the descriptor&#39;s exact entry point, ordered buffers and grid/block geometry. ROCm can execute a descriptor containing precompiled HSACO; HIP source and compiled HSACO are different kernel representations, so HIP source fails closed until hipRTC or an AOT compilation step produces a real code object. Vulkan SPIR-V codegen is also not confused with a native Vulkan dispatcher. This preserves one public SIMD/SIMT interface without pretending that every deployment bridge is already complete.</p><p>The AOT compiler bridge is gated independently from the runtime executor. Metal currently has a compatible compiler call bridge. WebGPU remains fully usable through the SIR runtime executor, but compiler-generated calls fall back until the legacy N-input/single-output FFI is replaced with the same descriptor contract. This prevents an ABI mismatch while preserving WebGPU&#39;s real runtime capability.</p><p>Backend capability failure never changes value semantics. For example, Metal has no native <code>f64</code> lane type, so Metal codegen rejects <code>Mask.compress</code> over <code>f64</code> and the planner must route it elsewhere rather than narrowing values to <code>float</code>. Native standalone Mask kernels use one warp/wave/simdgroup when possible. Wider fixed masks use the bounded whole-mask path rather than changing the result or requiring source-level backend branching.</p><p>The descriptor-based SIR runtime carries both outputs and performs the mask transport conversion. The older compiler GPU FFI bridge still accepts only one ordinary output buffer; it now rejects fixed/DynMask transport and multi-output graphs so dispatch falls back to CPU instead of risking a buffer overrun. It will be enabled for this path only after the same descriptor ABI is wired into that bridge.</p><p>All cross-lane operations (<code>toBitmask</code>, <code>any</code>, <code>all</code>, <code>countBits</code>, <code>firstSet</code>, and <code>compress</code>) use a proven native execution-lane group when possible and a bounded whole-mask legalization otherwise. No backend may independently reduce or compact each warp/wave and present that as a whole-mask result.</p><h2 id="_7-how-we-keep-backend-parity-safe" tabindex="-1">7) How we keep backend parity safe <a class="header-anchor" href="#_7-how-we-keep-backend-parity-safe" aria-label="Permalink to &quot;7) How we keep backend parity safe&quot;">​</a></h2><ul><li>Keep the language surface in <code>Tensor</code>/<code>Mask</code> operations and do not branch on backend in user code.</li><li>Treat SIMD width and GPU workgroup size as hints chosen by compiler/runtimes.</li><li>Use the docs’ operator tables and pipeline pages as the behavioral source of truth.</li><li>Validate important kernels both on CPU SIMD and GPU paths when changing contracts (especially reductions).</li></ul><h2 id="_8-why-this-matters" tabindex="-1">8) Why this matters <a class="header-anchor" href="#_8-why-this-matters" aria-label="Permalink to &quot;8) Why this matters&quot;">​</a></h2><p>When this contract is respected:</p><ul><li>compiler codegen can optimize one operation into many backends</li><li><code>graph fn</code> can stay backend-agnostic</li><li>benchmark deltas come from schedule/capability, not API drift</li></ul><p>If you want raw backend-specific tuning, do it behind a feature-gated call boundary without replacing this base contract.</p><h2 id="related-docs" tabindex="-1">Related docs <a class="header-anchor" href="#related-docs" aria-label="Permalink to &quot;Related docs&quot;">​</a></h2><ul><li><a href="/docs/guide/simd/">SIMD Overview</a></li><li><a href="/docs/guide/simd/sir-pipeline">SIR Pipeline</a></li><li><a href="/docs/guide/simd/tensor-mask">Tensor and Mask Types</a></li><li><a href="/docs/guide/gpu/">GPU Programming</a></li></ul>`,51)])])}const k=a(n,[["render",o]]);export{u as __pageData,k as default};
