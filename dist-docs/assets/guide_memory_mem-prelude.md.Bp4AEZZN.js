import{_ as a,o as t,c as i,ag as s}from"./chunks/framework.BGkQCay2.js";const k=JSON.parse('{"title":"Memory Prelude (Mem)","description":"","frontmatter":{},"headers":[],"relativePath":"guide/memory/mem-prelude.md","filePath":"guide/memory/mem-prelude.md"}'),o={name:"guide/memory/mem-prelude.md"};function n(l,e,d,r,h,c){return t(),i("div",null,[...e[0]||(e[0]=[s(`<h1 id="memory-prelude-mem" tabindex="-1">Memory Prelude (<code>Mem</code>) <a class="header-anchor" href="#memory-prelude-mem" aria-label="Permalink to &quot;Memory Prelude (\`Mem\`)&quot;">​</a></h1><p><code>Mem</code> is VexArch&#39;s single prelude boundary for allocation, reallocation, byte movement, and explicit destruction. It is available without an import, but it is intended for runtime, standard-library, allocator, and data-structure code. Most applications should use <code>Box&lt;T&gt;</code>, <code>Vec&lt;T&gt;</code>, <code>string</code>, or another owning type.</p><p><code>Mem</code> does not make raw memory safe. Every caller remains responsible for allocation extent, alignment, initialization, aliasing, and allocator pairing.</p><h2 id="core-allocation-api" tabindex="-1">Core allocation API <a class="header-anchor" href="#core-allocation-api" aria-label="Permalink to &quot;Core allocation API&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Function</th><th>Purpose</th></tr></thead><tbody><tr><td><code>Mem.alloc(size: usize): Ptr&lt;Opaque!&gt;</code></td><td>allocate <code>size</code> bytes, 16-byte aligned</td></tr><tr><td><code>Mem.allocAligned(size: usize, align: usize): Ptr&lt;Opaque!&gt;</code></td><td>allocate with a power-of-two alignment</td></tr><tr><td><code>Mem.realloc(p, oldSize, newSize): Ptr&lt;Opaque!&gt;</code></td><td>resize a block while preserving the common prefix</td></tr><tr><td><code>Mem.free(p, size): ()</code></td><td>free a block with its exact allocation size</td></tr></tbody></table><div class="language-vex vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vex</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> size </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> #</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sizeOf&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Header</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> usize</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> memory </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Mem</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">alloc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(size)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> header </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RawBuf</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(memory)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">header</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">zero</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(size)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">header</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">store&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">u32</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0x56455821</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Mem</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">free</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(memory, size)</span></span></code></pre></div><p><code>Mem.free</code> expects the exact allocation size. For <code>Ptr.allocN&lt;T&gt;(count)</code>, use <code>Ptr.freeN(count)</code> so the element count is converted to the correct byte size.</p><p>For checked dynamic layouts, <code>Layout</code> and its owning <code>Block</code> are also prelude types. They require no package import. <code>Block</code> records the exact size and alignment, frees itself through <code>Drop</code>, supports transactional <code>resize</code>, and can transfer ownership as a <code>(pointer, layout)</code> pair through <code>release</code>.</p><h2 id="heap-and-region-bound-allocation" tabindex="-1">Heap- and region-bound allocation <a class="header-anchor" href="#heap-and-region-bound-allocation" aria-label="Permalink to &quot;Heap- and region-bound allocation&quot;">​</a></h2><p>VexArch distinguishes temporary arena allocation from storage that must survive an arena reset.</p><table tabindex="0"><thead><tr><th>Function</th><th>Purpose</th></tr></thead><tbody><tr><td><code>Mem.heapAlloc(size)</code></td><td>allocate from the persistent slab/large heap</td></tr><tr><td><code>Mem.heapRealloc(p, oldSize, newSize)</code></td><td>resize persistent heap storage</td></tr><tr><td><code>Mem.currentRegion()</code></td><td>return the active allocator region, or null</td></tr><tr><td><code>Mem.regionAlloc(region, size)</code></td><td>allocate through a captured region; null selects heap behavior</td></tr><tr><td><code>Mem.regionRealloc(region, p, oldSize, newSize)</code></td><td>resize through the same captured region</td></tr></tbody></table><p>Containers capture <code>Mem.currentRegion()</code> when they are created and route their backing-buffer growth through that captured region. This prevents a container from silently changing allocator lifetime after construction.</p><p>These functions are public so prelude and low-level library types can share the same allocator contract. Application code should not select an arena/heap path manually unless it is implementing an owning abstraction.</p><h2 id="byte-operations" tabindex="-1">Byte operations <a class="header-anchor" href="#byte-operations" aria-label="Permalink to &quot;Byte operations&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Function</th><th>Semantics</th></tr></thead><tbody><tr><td><code>Mem.copy(dst, src, n)</code></td><td>copy <code>n</code> bytes; regions must not overlap</td></tr><tr><td><code>Mem.move(dst, src, n)</code></td><td>copy <code>n</code> bytes; overlapping regions are supported</td></tr><tr><td><code>Mem.set(dst, value, n)</code></td><td>fill <code>n</code> bytes with the low byte of <code>value</code></td></tr><tr><td><code>Mem.compare(a, b, n): i32</code></td><td>bytewise comparison</td></tr><tr><td><code>Mem.equals(a, b, n): bool</code></td><td>equality over <code>n</code> bytes</td></tr><tr><td><code>Mem.zero(dst, n)</code></td><td>fill <code>n</code> bytes with zero</td></tr></tbody></table><p>Prefer <code>RawBuf</code> or <code>Ptr&lt;T&gt;</code> methods when they better express the offset unit and element type. <code>Mem</code> is the shared primitive boundary beneath those wrappers.</p><h2 id="explicit-destruction" tabindex="-1">Explicit destruction <a class="header-anchor" href="#explicit-destruction" aria-label="Permalink to &quot;Explicit destruction&quot;">​</a></h2><div class="language-vex vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vex</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> owned </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> acquireResource</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// End this value&#39;s lifetime before the surrounding scope exits.</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Mem</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">drop</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(owned)</span></span></code></pre></div><p><code>Mem.drop&lt;T&gt;(value)</code> consumes the value and runs its ownership-aware destruction path. Use it only for intentional early destruction. Values are otherwise dropped automatically at scope exit; calling <code>Mem.drop</code> and then using the moved value is invalid.</p><p>The raw ownership primitive used to implement <code>Mem.drop</code> is compiler-owned and prelude-only. It is not a developer-facing intrinsic.</p><h2 id="size-metadata-compatibility-path" tabindex="-1">Size-metadata compatibility path <a class="header-anchor" href="#size-metadata-compatibility-path" aria-label="Permalink to &quot;Size-metadata compatibility path&quot;">​</a></h2><p><code>Mem.allocCompat(size)</code>, <code>Mem.allocZeroedCompat(count, elementSize)</code>, <code>Mem.freeCompat(p)</code>, and <code>Mem.reallocCompat(p, newSize)</code> form the canonical path for APIs that cannot carry allocation size metadata to deallocation. They use an inline size header and therefore give up the faster exact-size path. New owning abstractions should preserve a <code>Layout</code> and use <code>Block</code> or <code>Mem.alloc/realloc/free</code>.</p><p>There is no <code>std/mem</code> package and no parallel set of free functions. <code>Mem.*</code> is the single source of truth across the compiler prelude, VexArch, standard library, and application code.</p><h2 id="unbounded-loops-and-arena-safety" tabindex="-1">Unbounded loops and arena safety <a class="header-anchor" href="#unbounded-loops-and-arena-safety" aria-label="Permalink to &quot;Unbounded loops and arena safety&quot;">​</a></h2><p><code>Mem.unboundedLoop()</code> is a zero-runtime-cost compiler hint for a loop whose exit depends on runtime state and cannot be proven bounded:</p><div class="language-vex vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vex</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">while</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> keepRunning {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    Mem</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">unboundedLoop</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    processNextEvent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The compiler already recognizes <code>loop {}</code> and statically true <code>while</code> conditions. Use the hint only when a genuinely runtime-gated loop would otherwise look bounded. It prevents scope-root arena allocation from accumulating indefinitely in a loop that may never exit.</p><h2 id="freestanding-boundary" tabindex="-1">Freestanding boundary <a class="header-anchor" href="#freestanding-boundary" aria-label="Permalink to &quot;Freestanding boundary&quot;">​</a></h2><p><code>Mem</code> is a VexArch API, not a promise that Vex source depends on host libc allocation. Its platform implementation may use OS/native system services, a custom allocator, or a freestanding backend. Prelude and standard-library code remain expressed against the same <code>Mem</code> contract instead of importing <code>malloc/free</code> directly.</p><p>This separation is essential for freestanding builds: platform-specific memory provision lives below VexArch, while Vex ownership and container code remains portable above it.</p><h2 id="safety-checklist" tabindex="-1">Safety checklist <a class="header-anchor" href="#safety-checklist" aria-label="Permalink to &quot;Safety checklist&quot;">​</a></h2><ol><li>Preserve the exact size and allocator/region that created each block.</li><li>Do not read uninitialized bytes or access memory after free.</li><li>Use <code>Mem.move</code> when source and destination may overlap.</li><li>Validate alignment before constructing typed or SIMD views.</li><li>Prefer RAII owners; expose raw <code>Mem</code> operations only inside a small audited implementation.</li></ol><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to &quot;Related&quot;">​</a></h2><ul><li><a href="/docs/guide/memory/ptr-t"><code>Ptr&lt;T&gt;</code></a></li><li><a href="/docs/guide/memory/span-t"><code>Span&lt;T&gt;</code></a></li><li><a href="/docs/guide/memory/rawbuf">RawBuf</a></li><li><a href="/docs/guide/memory/ownership">Ownership</a></li><li><a href="/docs/guide/freestanding">Freestanding</a></li></ul>`,34)])])}const u=a(o,[["render",n]]);export{k as __pageData,u as default};
