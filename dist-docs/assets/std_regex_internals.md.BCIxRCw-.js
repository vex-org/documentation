import{_t as e,it as t,n,rt as r}from"./chunks/framework.B5d4da8a.js";var i=JSON.parse(`{"title":"Regex Engine Internals","description":"","frontmatter":{},"headers":[],"relativePath":"std/regex/internals.md","filePath":"std/regex/internals.md"}`),a={name:`std/regex/internals.md`};function o(n,i,a,o,s,c){return e(),r(`div`,null,[...i[0]||=[t(`<h1 id="regex-engine-internals" tabindex="-1">Regex Engine Internals <a class="header-anchor" href="#regex-engine-internals" aria-label="Permalink to “Regex Engine Internals”">​</a></h1><h2 id="pipeline" tabindex="-1">Pipeline <a class="header-anchor" href="#pipeline" aria-label="Permalink to “Pipeline”">​</a></h2><div class="language-text"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>source → bounded parser → AST → Thompson bytecode (Prog)</span></span>
<span class="line"><span>                                  ├─ literal/prefix metadata</span></span>
<span class="line"><span>                                  └─ one-pass DFA when provably deterministic</span></span></code></pre></div><p>The AST is compile-only and is not retained by the returned <code>Regex</code>. Thompson fragments also allocate no side storage: each <code>Frag</code> is three <code>i32</code> values and its unresolved edges form an intrusive linked list in the unfinished program&#39;s target cells. After patching, those same cells contain ordinary instruction targets. The validated instruction budget reserves the four persistent SoA bytecode streams once, avoiding geometric reallocation and copy traffic. Before publication, <code>Prog.isWellFormed</code> rejects unresolved intrusive links, invalid opcodes, mismatched SoA lengths, bad class indices and out-of-range targets. Temporary compiler state therefore cannot leak into execution.</p><p>This gives per-call, thread-local compilation state without a process-global cache, runtime FFI allocator, or forced arena mode. Applications should compile a pattern once and share the resulting immutable <code>Regex</code>.</p><p>The parser fails closed for unknown flags, malformed inline flags, unsupported scoped flags, capture-slot overflow, excessive repeats, oversized patterns and programs whose instruction or capture-scratch budget would be unsafe.</p><h2 id="execution-engines" tabindex="-1">Execution engines <a class="header-anchor" href="#execution-engines" aria-label="Permalink to “Execution engines”">​</a></h2><h3 id="literal-and-required-literal-filters" tabindex="-1">Literal and required-literal filters <a class="header-anchor" href="#literal-and-required-literal-filters" aria-label="Permalink to “Literal and required-literal filters”">​</a></h3><p>Pure literals use <code>str</code> search directly. The compiler may also publish a conservatively proven <code>requiredLiteral</code>. It is a rejection filter only; a literal from an optional group or one alternation branch is never treated as globally required. Exact <code>.*literal.*</code> shapes can complete directly.</p><p>Multi-byte prefixes use the built-in <code>str.indexOf</code>/<code>contains</code> implementation, which inherits Vex&#39;s native SIMD lowering. Sparse prefix hits are verified by a bounded candidate engine. After at most two input lengths of speculative work, execution falls back to the parallel Thompson state set. Dense adversarial prefixes therefore remain linear.</p><h3 id="one-pass-dfa" tabindex="-1">One-pass DFA <a class="header-anchor" href="#one-pass-dfa" aria-label="Permalink to “One-pass DFA”">​</a></h3><p>Eligible programs receive a 256-way transition table. Empty acceptance is stored separately from byte transitions, so <code>a*</code> does not turn every non-<code>a</code> byte into a match. Position-dependent assertions remain explicit states and are evaluated at the exact input offset. Assertion table targets use a negative sentinel, leaving the ordinary hot loop as a direct table lookup.</p><p>Multiline programs currently use the general Thompson engine. If branch byte sets overlap, one-pass construction fails closed and the same fallback is used.</p><h3 id="thompson-boolean-engine" tabindex="-1">Thompson boolean engine <a class="header-anchor" href="#thompson-boolean-engine" aria-label="Permalink to “Thompson boolean engine”">​</a></h3><p>Programs with at most 64 instructions use a register-resident <code>u64</code> state set and iterate set bits with <code>Math.ctz</code>. All possible unanchored starts coexist in the same state set, so the input is consumed once instead of restarting the NFA for every byte. Larger programs use two reusable vectors per call. Both paths are linear in input length for a fixed program and contain no recursive backtracking.</p><h3 id="prioritized-pike-capture-engine" tabindex="-1">Prioritized Pike capture engine <a class="header-anchor" href="#prioritized-pike-capture-engine" aria-label="Permalink to “Prioritized Pike capture engine”">​</a></h3><p><code>Regex.exec</code> uses an ordered Pike VM over the same bytecode. Split order carries greedy/lazy priority; a candidate match is retained while higher-priority live threads are allowed to extend it. Capture <code>Save</code> slots travel with each thread.</p><p>Mutable VM scratch belongs to <code>Match</code>, not <code>Regex</code>. A compiled regex can therefore be shared by concurrent callers, while a caller that reuses its <code>Match</code> value pays workspace growth only on the first call. Epsilon generations avoid per-thread clears and wrap safely.</p><h2 id="complexity-and-limits" tabindex="-1">Complexity and limits <a class="header-anchor" href="#complexity-and-limits" aria-label="Permalink to “Complexity and limits”">​</a></h2><ul><li>Boolean search/full match: linear in input for a fixed bytecode program.</li><li>Capture execution: prioritized Thompson/Pike simulation; no catastrophic recursive backtracking.</li><li>Repeat count: at most 1000.</li><li>Capture groups: at most 127 (the bytecode&#39;s one-byte Save-slot format).</li><li>Group nesting: at most 256.</li><li>Pattern source: at most 1 MiB.</li><li>Compiled program: at most 65,536 instructions.</li><li>Capture workspace: at most 16,777,216 <code>i32</code> values.</li></ul><h2 id="metadata" tabindex="-1">Metadata <a class="header-anchor" href="#metadata" aria-label="Permalink to “Metadata”">​</a></h2><p><code>ProgMeta</code> contains exact minimum length, anchored-start information, pure and prefix literals, a conservative required literal and the exact literal-search-only classification. Metadata is never allowed to change regex semantics; uncertain cases fall back to the complete engine.</p>`,22)]])}var s=n(a,[[`render`,o]]);export{i as __pageData,s as default};