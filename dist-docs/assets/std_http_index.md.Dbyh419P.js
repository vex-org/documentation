import{_t as e,it as t,n,rt as r}from"./chunks/framework.B5d4da8a.js";var i=JSON.parse(`{"title":"HTTP & Client Overview","description":"","frontmatter":{},"headers":[],"relativePath":"std/http/index.md","filePath":"std/http/index.md"}`),a={name:`std/http/index.md`};function o(n,i,a,o,s,c){return e(),r(`div`,null,[...i[0]||=[t(`<h1 id="http-client-overview" tabindex="-1">HTTP &amp; Client Overview <a class="header-anchor" href="#http-client-overview" aria-label="Permalink to “HTTP &amp; Client Overview”">​</a></h1><p>For routed synchronous HTTP/1-only services, <code>App&lt;{HTTP_V1}&gt;.new()</code> selects a compile-time capability profile. <code>App</code> and its <code>Group</code> values retain one exact const-generic mask while H2, WebSocket, async-route and streaming execution engines are excluded from the emitted program. Capabilities can be composed, for example <code>App&lt;{HTTP_V1 | WEBSOCKET}&gt;</code>; unsupported registrations fail at compile time. The exact zero-residue listener currently applies to <code>HTTP_V1</code>; other valid masks use the full worker kernel until per-bit worker specialization lands. Plain <code>App.new()</code> remains the <code>HTTP_FULL</code> compatibility default. The choice adds no per-request runtime feature branch. See <a href="./fiber#compile-time-http1-profile">Fiber Framework</a>.</p><p>The <code>http</code> package provides Vex-native HTTP parsers, client/server foundations, and the <code>http/fiber</code> application framework. Its API direction takes inspiration from modern Go/Rust libraries and Fiber v3, while ownership, arenas, scheduling, and hardware acceleration remain native Vex mechanisms.</p><p>HTTP/1 is under active production hardening. Fiber WebSocket HTTP/1 upgrade has a bounded worker-owned connection state machine with explicit lifecycle hooks; live interoperability/load gates remain open. HTTP/2 has a unified bounded protocol/application owner and a live cleartext prior-knowledge path for synchronous buffered routes, async request routes and bounded streaming responses. TLS ALPN, request-body streaming, WebSocket-over-H2 and external interoperability remain open. HTTP/3/QUIC is a planned later phase after the shared HTTP/1, application, scheduler, and HTTP/2 contracts stabilize.</p><h2 id="key-capabilities" tabindex="-1">Key Capabilities <a class="header-anchor" href="#key-capabilities" aria-label="Permalink to “Key Capabilities”">​</a></h2><ol><li><strong>Fiber v3 Framework Layer</strong>: Complete web framework abstractions (<code>App</code>, <code>Ctx</code>, <code>Router</code>) supporting <code>:param</code> binding and <code>*wildcard</code> routes.</li><li><strong>Zero-Copy Parsers</strong>: Request parsers strictly slice bytes (<code>Span&amp;lt;u8&amp;gt;</code>) from the underlying socket without any intermediate GC allocation for headers or values.</li><li><strong>WebSockets (<code>http/ws</code>)</strong>: Fiber-integrated HTTP/1 RFC 6455 upgrade with worker-owned bounded queues, strict framing, lifecycle hooks, non-blocking partial I/O, bounded route-level subprotocol selection and idle timeout ownership. An automated loopback gate covers coalesced upgrade/frame handoff, request-arena lifetime, protocol callbacks and close drain; external interoperability/load gates remain.</li><li><strong>HTTP/2 (<code>http/parser</code>, <code>http/fiber</code>)</strong>: Bounded protocol foundations plus live cleartext prior-knowledge serving for synchronous buffered and async request routes: exact fragmented-preface selection, non-blocking worker queues, negotiated and type-specific frame checks with typed RFC stream/connection error scope, separate local-configuration failures, and extension-safe unknown-frame discard that still enforces field-block exclusivity. Transactional SETTINGS, typed whole-block HPACK, complete Huffman decoding, explicit header/table limits, checked stream windows, atomic capacity-aware control encoders and connection-owned CONTINUATION/SETTINGS-ACK/GOAWAY sequencing. DATA ownership is transactional across exact payload shape, checked padding, borrowed application bytes, scheduler budget, connection/stream windows and END_STREAM state. Late DATA is discarded without losing connection credit, while closed-stream WINDOW_UPDATE races are accepted without reviving state; generic frame APIs cannot bypass it. WINDOW_UPDATE has the same exact-frame ownership across parsing/encoding, sequencing, scoped overflow, credit and scheduler wakeup. RST_STREAM likewise owns exact payload/error decoding, idle-stream scope, sequencing, retirement and complete encoding; failed output commits nothing. SETTINGS window deltas are overflow-safe and directional. Explicit endpoint roles enforce local/peer stream parity, monotonic peer IDs, capacity, non-wrapping ID exhaustion and graceful GOAWAY drain/retry boundaries before materialization. Dedicated GOAWAY receive/send methods bind exact payload/debug bytes, sequencing, the non-increasing boundary, retry-safe retirement and complete encoding; failed output cannot begin draining. PING uses exact opaque payloads and a preallocated bounded correlation set; duplicate/full/failed sends consume no token, matched ACKs release one, and unmatched ACKs cannot corrupt another liveness probe. RFC 9218 priority fields and PRIORITY_UPDATE are bounded and typed. Dedicated receive/send methods atomically bind exact parsing or encoding, client-only sequencing and scheduler mutation; generic frame APIs cannot bypass them. A pre-reserved urgency/incremental scheduler providing serial/round-robin service, bounded starvation and no allocation during selection/completion. Deprecated RFC 7540 PRIORITY is isolated behind compatibility-only exact-frame methods: it accepts any stream state without materializing unknown IDs, preserves error scope and never enters the RFC 9218 scheduler. <code>H2ProtocolState</code> is the recommended integration boundary: it atomically owns admission, recyclable stream slots, active plus idle priority capacity, flow-aware writable leases, reset, GOAWAY retirement and abort. It also owns bounded fragmented header reassembly and the connection-lifetime HPACK table; stream errors do not desynchronize later streams, while compression errors fail the connection closed. Successfully decompressed fields then cross a strict request/response/CONNECT/push/trailer semantic gate; invalid names/values, connection fields and pseudo-header/message-phase violations retire only the malformed stream before application delivery. It also owns SETTINGS lifecycle: peer changes apply immediately, local snapshots commit in FIFO ACK order, initial-window deltas are transactional, and acknowledged HPACK table reductions are enforced at the next field-block boundary. Wire-level unlimited defaults remain separate from configured local memory/concurrency ceilings. <code>H2ProtocolState.recvFrame</code> is the single complete-frame receive boundary: known types reach their dedicated transaction, unknown extensions remain connection-sequenced, and HEADERS performs peer admission internally. Refused/GOAWAY-ignored field blocks still advance HPACK before their typed result is returned, so transport cannot split admission from compression. Completed field results retain validated message kind, pseudo-header positions and END_STREAM. Content-Length lists and DATA content totals are owned per stream in both directions; padding consumes flow credit but never content length, while mismatches fail without contaminating the connection. connection-owned <code>recvBytes</code> handles fragmented caller-owned input with exact consumed-prefix reporting. Valid payloads remain zero-copy; rejected stream payloads are discarded incrementally without frame-sized allocation before parsing resumes at the next pipelined frame. Outbound field sections use the same owner: semantic phase, stream/connection transition, negotiated limits, HPACK table and output commit atomically. Indexing policy is explicit, credentials/cookies default to never-indexed, Huffman is used only when smaller, and generic frame APIs reject header-bearing sends. Successful blocks return an allocation-free cursor that maps the contiguous payload to negotiated-size HEADERS and CONTINUATION frames without copying. Server <code>sendPushPromise</code> uses the same transaction for the carrier, even promised ID, <code>ReservedLocal</code> slot, scheduler capacity and HPACK bytes; its cursor includes the four-byte promised-ID prefix in exact frame/queue accounting. Fiber contains no dormant H2 side transport; prior knowledge installs the same cancellable non-blocking owner used by the protocol/application tests. Async request handlers publish through a bounded worker-local completion lane while the event loop retains the socket. Exact connection-generation identity rejects late results after reset, deadline, shutdown or fd reuse.</li><li><strong>Middlewares</strong>: Composable request handlers for logging and CORS. Static serving is a first-class <code>App.static</code> / <code>Group.static</code> route, not a duplicate middleware path.</li></ol><h2 id="verified-http-1-behavior" tabindex="-1">Verified HTTP/1 behavior <a class="header-anchor" href="#verified-http-1-behavior" aria-label="Permalink to “Verified HTTP/1 behavior”">​</a></h2><ul><li>strict request framing rejects ambiguous length/transfer-encoding forms;</li><li>bounded incremental fixed-length and chunked bodies;</li><li>fragmented chunks and trailers with exact consumed/decoded progress;</li><li>preserved HTTP/1 pipelining suffixes and keep-alive ordering;</li><li>frozen immutable routing with worker-local request scratch;</li><li>route-local middleware is held once in a startup-owned flat router pool; frozen radix nodes retain route identity only, avoiding per-route middleware allocation/copy and duplicate handler ownership;</li><li>explicit native-handle ownership transfer in multi-worker servers;</li><li>request arenas reset handler temporaries without owning persistent connection state;</li><li>monotonic absolute HTTP/1 read deadlines resist slowloris drip-feeding;</li><li>idle peers are retired by bounded worker maintenance ticks;</li><li>response drains are bounded across partial writes and socket backpressure;</li><li>one shared <code>AppConfig.maxConnections</code> budget sheds excess accepted peers before parser/state allocation and reclaims capacity on exact descriptor ownership;</li><li>response serialization uses reusable one-pass storage with zero allocations after warmup on the verified common path.</li><li>fixed-width <code>Mask&lt;16&gt;</code> SIMD delimiter scans avoid dynamic-mask allocation in the HTTP/1 parser hot path; request/response correctness does not depend on a C or Rust parser implementation.</li><li>the live Fiber and canonical request parsers agree on exact HTTP version, request-target, framing, and <code>Connection</code> token rules; those shared security boundaries are regression-tested rather than inferred from one parser alone.</li><li>Fragmented <code>Content-Length</code> and chunked request bodies share one incremental body state, so slow uploads retain their header once rather than repeatedly copying the complete partial request.</li><li>Fiber appends responses directly into connection-owned reusable storage; <code>Response.appendEncodedTo(out)</code> exposes the same caller-owned batching contract for advanced servers without raw socket manipulation. Its <code>AppConfig.responseBatchBytes</code> high-water mark drains long pipelined bursts before aggregate response storage can grow with the peer&#39;s request queue; exceptional response buffers are released after drain rather than retained for the entire keep-alive connection.</li><li><code>App.listenWithContext</code> provides context-native cancellation, descriptor cleanup, worker joining, and same-port restart safety.</li><li><code>App.static</code> and <code>Group.static</code> canonicalize both the configured root and candidate before opening a file. Explicit parent traversal and symlinks that resolve outside the published root are rejected.</li><li>Known-length static and <code>Ctx.sendFile</code> responses are framed in order and streamed from their opened descriptor through Fiber&#39;s reusable 64 KiB worker scratch buffer, rather than being materialized as response-sized strings.</li><li>Dynamic HTTP/1 responses use <code>App.getStream</code> / <code>postStream</code> and a bounded <code>ResponseStreamWriter</code>: canonical chunk framing, terminal signaling and a single full-response deadline remain Fiber-owned. Stream routes preserve the normal frozen router and middleware chain, while prior pipelined replies drain before a producer starts.</li><li>Response framing has one authority: <code>HEAD</code> reports the equivalent <code>GET</code> length without sending bytes, while <code>1xx</code>, <code>204</code>, <code>205</code>, and <code>304</code> never carry content. Application headers cannot inject a competing <code>Content-Length</code>, <code>Transfer-Encoding</code>, or <code>Connection</code> field.</li></ul><p>See <a href="./fiber">Fiber Framework</a> for the live server model and <code>lib/std/http/VISION.md</code> in the source tree for open production gates.</p><hr><h2 id="http-client" tabindex="-1">HTTP Client <a class="header-anchor" href="#http-client" aria-label="Permalink to “HTTP Client”">​</a></h2><p>The outbound HTTP/1 client is a bounded async exchange over an already-resolved <code>SocketAddr</code>. A request carries its HTTP authority; the endpoint carries its TCP peer. This prevents a blocking DNS operation from being accidentally hidden inside a scheduler task.</p><div class="language-vex"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">vex</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">import { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ClientRequest</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } from </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;http&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">import { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Context</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } from </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;context&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">import { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">IpAddr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Ipv4Addr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">SocketAddr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } from </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;net&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">import { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Duration</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } from </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;time&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> endpoint </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SocketAddr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">IpAddr</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">V4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Ipv4Addr</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">loopback</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()), </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8080</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> context </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Context</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">background</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">withTimeout</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Duration</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">seconds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> request </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ClientRequest</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;GET&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;service.example&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/health&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> response </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> request</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">send</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(endpoint, </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">context)</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>The encoder owns <code>Host</code>, <code>Content-Length</code>, and <code>Connection</code>. Stateless client calls use <code>Connection: close</code>; an explicit caller-owned <code>HttpSession</code> uses <code>keep-alive</code> only after a proven response boundary. The client uses one canonical framing decision shared with the parser: a single <code>Content-Length</code>, exact <code>Transfer-Encoding: chunked</code>, or close-delimited body. Ambiguous framing and configured-size violations are errors. There is no implicit shared pool.</p><p>Redirects are equally explicit. <code>sendFollowing</code> accepts a finite <code>HttpRedirectPolicy</code> and reuses the caller-resolved endpoint only for same-origin hops. <code>sendFollowingResolved</code> requires an async <code>HttpRedirectResolver</code> before any cross-origin connection and strips authorization/cookie credentials at that boundary. RFC 3986 relative resolution, method/body replay rules, unsupported schemes and limit failures are documented and tested; ordinary <code>send</code> still means exactly one exchange.</p><p>Owning responses can opt into pure-Vex <code>br</code>, <code>zstd</code>, <code>gzip</code>, and <code>deflate</code> decoding with <code>acceptCompressed</code> plus <code>decodeContent</code>. Transferred and decoded byte ceilings are independent, coding chains are bounded and reversed strictly, and failure leaves the raw response unchanged. Streamed decoding is not emulated with hidden whole-body buffering.</p><p>Interim responses have an explicit count bound. Upgrade and successful <code>CONNECT</code> responses are rejected until a public API can transfer sole stream ownership. <code>HEAD</code>, <code>204</code>, <code>205</code>, and <code>304</code> follow their bodyless response semantics; <code>205</code> may consume only a provably empty framed body.</p><p>Chunked responses are safe across arbitrary TCP read boundaries: an incomplete chunk-size line, data CRLF, or trailer is retained in a small bounded framing buffer, while already-decoded payload goes directly to the selected response destination. This applies equally to buffered <code>send</code> and <code>sendInto</code>.</p><p>The client already routes its request/response I/O through the shared, context-required <code>io::AsyncReader</code> / <code>AsyncWriter</code> boundary. This preserves cancellation and backpressure semantics today while reserving one adapter seam for TLS and streaming bodies; it is not a claim of pooling or upgrade support.</p><p>See <a href="./client">HTTP Client</a> for limits, cancellation behavior, supported surface, and deliberate current boundaries.</p>`,20)]])}var s=n(a,[[`render`,o]]);export{i as __pageData,s as default};